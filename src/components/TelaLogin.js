import React from "react";
import { AppBar, Toolbar } from "@mui/material";
const Header = () => {
  return (
    <AppBar position="static">
      <Toolbar>Tela de login</Toolbar>
    </AppBar>
  );
};

export default Header;
